name: Deploy Docs

on:
  push:
    branches: [main]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install awesome-pages mkdocs mkdocs-material pyodide-mkdocs-theme
      - run: mkdocs gh-deploy --force
